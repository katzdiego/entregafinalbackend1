<h2>Listado de productos</h2>

<ul>
  {{#each products}}
    <li>
      <h3>{{this.title}}</h3>
      <p>{{this.description}}</p>
      <p><strong>Precio:</strong> ${{this.price}}</p>
      <a href="/product/{{this._id}}">Ver detalle</a>
    </li>
  {{/each}}
</ul>